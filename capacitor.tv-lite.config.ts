import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.radiofm.tvlite',
    appName: 'Radio Pro TV Lite',
    webDir: 'dist-tv-lite',
    bundledWebRuntime: false
};

export default config;
